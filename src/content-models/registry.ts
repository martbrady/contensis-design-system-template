import type { ContensisContentModelSpec } from './types';

export const contentModelRegistry: Record<string, ContensisContentModelSpec> = {
  // Add component content models here.
  // See CLAUDE.md for field type conventions.
};
